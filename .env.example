# 🔐 Replace with your actual OpenAI API key before running
OPENAI_API_KEY="your_openai_api_key_here"

# OPENAI_MODEL_NAME="gpt-4o-mini"

# Or, set per-agent models (these names must match what you use in os.getenv in each agent's code):
CONTEXT_EXTRACTOR_MODEL="gpt-4o-mini"
SCENARIO_GENERATOR_MODEL="gpt-4o-mini"
PROBABILITY_COLLECTOR_MODEL="gpt-4o-mini"
TRANSACTION_LOGIC_MODEL="gpt-4o-mini"
AGGREGATION_MODEL="gpt-4o-mini"
EXPLANATION_MODEL="gpt-4o-mini"

LITELLM_DEBUG=1